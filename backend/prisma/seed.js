async function main() {
  // TODO: Add minimal development seed data after core use cases are implemented.
  // Keep seed data small, deterministic, and safe to run more than once.
  console.log('Seed placeholder: no data inserted.');
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
