import { notImplemented } from '../_shared/notImplemented.js';

export const authController = {
  register: notImplemented('Auth registration'),
  login: notImplemented('Auth login'),
  forgotPassword: notImplemented('Password reset request'),
  resetPassword: notImplemented('Password reset')
};
